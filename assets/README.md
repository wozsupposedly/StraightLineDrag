Put your app icons here.

For macOS packaging with Electron Forge:

- `assets/icon.icns` for the app and DMG

Forge uses `assets/icon` as the base icon path in `forge.config.js`, so the `.icns` file should be named exactly `icon.icns`.
